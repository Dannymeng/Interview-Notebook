# 03 - 数组中重复的数字

## 描述

找出数组中重复的数字。


在一个长度为 n 的数组 nums 里的所有数字都在 0～n-1 的范围内。数组中某些数字是重复的，但不知道有几个数字重复了，也不知道每个数字重复了几次。请找出数组中任意一个重复的数字。

**示例 1：**

```
输入：
[2, 3, 1, 0, 2, 5, 3]
输出：2 或 3
```





## 解题

```java
//hash表：时间复杂度：时间O(n)，空间O(n)
class Solution {
    public int findRepeatNumber(int[] nums) {
        HashSet<Integer> set = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if(set.contains(nums[i]))
                return nums[i];
            set.add(nums[i]);
        }
        return 0;
    }
}
```



```java
//原地置换：索引与数字的关系，时间O(n)，空间O(1)
//https://leetcode-cn.com/problems/shu-zu-zhong-zhong-fu-de-shu-zi-lcof/solution/yuan-di-zhi-huan-shi-jian-kong-jian-100-by-derrick/
//注释：该方法有个bug,当数组中有数字太大会出现越界，[2, 10000,1, 0, 2, 5, 3,10000]
@Test
public void findRepeatNumber(int[] nums) {
    int temp;
    for(int i = 0;i<nums.length;i++){
        if(nums[i]!=i){
            if(nums[i] == nums[nums[i]]){
                System.out.println(nums[i]);
            }
            temp = nums[i];
            nums[i] = nums[temp];
            nums[temp] = temp;
        }
    }
}
```

